# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'openShutterVideo' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for openShutterVideo
  pod 'OpenCV', '~> 3.0.0'

  target 'openShutterVideoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'openShutterVideoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
